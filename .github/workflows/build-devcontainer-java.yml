name: build-devcontainer-java

on:
  push:
    branches:
      - main
    paths:
      - .dockerignore
      - dockerfiles/mise.java.toml
      - dockerfiles/Dockerfile.java
      - scripts/vscode-server-install.sh
      - .github/workflows/build-devcontainer-java.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04-arm

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Get short commit SHA
        id: vars
        run: echo "short_sha=${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: dockerfiles/Dockerfile.java
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            songhuangcn/devcontainer-java:commit-${{ steps.vars.outputs.short_sha }}
            songhuangcn/devcontainer-java:latest
