# IMAGE_NAME=songhuangcn/devcontainer
# IMAGE_TAG=latest

# OPENCODE_PORT=4096

# MULTICA_TOKEN=mul_replace-with-your-personal-access-token

# 这台本地实例统一的名字：Multica runtime 和 Claude Code Remote Control 的会话名
# 前缀都读这一个变量（默认都是 devcontainer.local）。不能不配——两者默认都会取
# 容器 hostname，而 Docker 默认给的 hostname 是随机容器 ID，每次重建都变，不是
# 人可读、也不稳定。只有部署完全独立的另一套本地实例时才需要覆盖它，同时也要把
# MULTICA_DAEMON_ID 换成新的 UUID，否则会复用已有 agent 绑定的 Multica runtime。
# DEVICE_NAME=devcontainer.local
# MULTICA_DAEMON_ID=019fffb2-a6d0-7045-8ff5-9b73fe675dd1
