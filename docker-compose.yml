name: workspace-devcontainer
services:
  app:
    # 默认拉取预构建镜像；改 Dockerfile 后用 `make build` 本地构建同名镜像。
    image: ${IMAGE_NAME:-songhuangcn/devcontainer}:${IMAGE_TAG:-latest}
    build: .
    env_file:
      - .env
    environment:
      DOCKER_HOST: tcp://docker:2375
    # depends_on:
    #   docker:
    #     condition: service_healthy
    init: true
    ports:
      - "${OPENCODE_PORT:-4096}:${OPENCODE_PORT:-4096}"
    #   - "2222:2222"
    entrypoint: ""
    command: opencode web --hostname 0.0.0.0 --port ${OPENCODE_PORT:-4096}
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:$${OPENCODE_PORT:-4096}/global/health >/dev/null"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 10s
    volumes:
      - ..:/workspace
      # 只持久化用户数据目录；.bashrc/.profile 等 shell 启动文件继续使用镜像内版本。
      - ./data/.agents:/home/ubuntu/.agents
      - ./data/.cache:/home/ubuntu/.cache
      - ./data/.claude:/home/ubuntu/.claude
      - ./data/.codex:/home/ubuntu/.codex
      - ./data/.config:/home/ubuntu/.config
      - ./data/.copilot:/home/ubuntu/.copilot
      - ./data/.lark-cli:/home/ubuntu/.lark-cli
      - ./data/.local:/home/ubuntu/.local
      - ./data/.npm:/home/ubuntu/.npm
      - ./data/.ssh:/home/ubuntu/.ssh
      - ./data/.vscode-server:/home/ubuntu/.vscode-server
      - type: bind
        source: ./data/.claude.json
        target: /home/ubuntu/.claude.json
        bind:
          create_host_path: false
      - type: bind
        source: ./data/.gitconfig
        target: /home/ubuntu/.gitconfig
        bind:
          create_host_path: false
  docker:
    image: docker:28-dind
    privileged: true
    environment:
      DOCKER_TLS_CERTDIR: ""
    volumes:
      - ..:/workspace
      - docker-data:/var/lib/docker
    healthcheck:
      test: ["CMD", "docker", "info"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  docker-data:
