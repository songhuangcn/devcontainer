{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "*": {
      "*": "allow"
    }
  },
  "disabled_providers": ["opencode"],
  // 用来给会话自动取名的模型，不设置的话会自己猜测 nano 等小模型，如果代理站没有，就会取名失败
  // 必须填代理站真实存在的模型；可用列表见 /models 接口
  "small_model": "openai/gpt-5.4-mini",
}
